# V7 Best Candidate ParaView Outputs

Open `v7_best_particles.pvd` for time-resolved particle animation, or `v7_best_trajectories.vtp` for full trajectories.

Note: the visible path is a spiral channel; the `stepwise` part of the V7 candidate is modeled as late segmented DEP activation rather than a serrated physical wall shape.
